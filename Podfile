# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Ripple Go' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    pod 'Firebase/Core'
    pod 'Firebase/Auth'
    pod 'Firebase/Database'
    pod 'GoogleMaps'
    pod 'GooglePlaces'
  # Pods for Ripple Go

  target 'Ripple GoTests' do
    inherit! :search_paths
    # Pods for testing

  end

  target 'Ripple GoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
